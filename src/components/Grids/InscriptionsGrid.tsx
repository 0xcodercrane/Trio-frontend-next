import { Inscription } from '@/types/database.types';

export default function InscriptionsGrid({ inscriptions }: { inscriptions: Inscription[] }) {
  return (
    <div>
      <h3>InscriptionsGrid Unimplemented</h3>
    </div>
  );
}
